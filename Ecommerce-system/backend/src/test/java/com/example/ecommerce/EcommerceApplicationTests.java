package com.example.ecommerce;

import com.example.ecommerce.mbg.mapper.UserrMapper;
import com.example.ecommerce.mbg.model.Userr;
import com.example.ecommerce.service.UserrService;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class EcommerceApplicationTests {
/*

    @Autowired
    UserrService userrService;

    @Autowired(required = false)
    UserrMapper userrMapper;
    @Test
    void contextLoads() {
        String name="123";
        String pass="234";
        String text="空";
        String tele="132";
        Userr userr = new Userr();
        userr.setUserid(name);
        userr.setUsertelephone(tele);
        userr.setUserpassword(pass);
        userr.setUseraddress(text);
        System.out.println(userr);

        userrMapper.insert(userr);
    }
*/

}
